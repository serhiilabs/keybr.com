name: typer

services:
  app:
    image: ghcr.io/serhiilabs/typer-app:latest
    env_file: .env
    ports:
      - "3100:3000"
      - "3101:3001"
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
